"""bdc-takeoff-proto: one IR for all inputs; extraction never forks by source type."""

IR_VERSION = "1.0-proto"
RUN_MODE = "PROTOTYPE"
DEFAULT_SNAP_IN = 1.0 / 32.0  # inches at drawing scale; per-project CLI arg
